<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:tint">@color/navText</item>
        <item name="android:textColor">@color/navText</item>
        <item name="android:navigationBarColor">@color/listViewBackground</item>
        <item name="colorPrimary">@color/navBackground</item>
        <item name="colorPrimaryDark">@color/navBackground</item>
        <item name="android:windowBackground">@color/listViewBackground</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>